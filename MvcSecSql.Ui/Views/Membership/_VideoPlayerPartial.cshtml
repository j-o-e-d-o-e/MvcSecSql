@model VideoViewModel

<div class="panel">
    @if (Model.Video.Url != null)
    {
        @*<div id="video" class="video-margin">&nbsp;</div>*@
        <video id="video" class="video-js vjs-default-skin vjs-big-play-centered vjs-16-9" controls preload="auto"> </video>
        <div id="hiddenUrl" hidden="hidden">@Model.Video.Url</div>
        <div id="hiddenImageUrl" hidden="hidden">@Model.Video.Thumbnail</div>
    }

    <div class="panel-body">
        <h2 class="text-dark">@Model.Video.Title</h2>
        <p class="text-dark">
            <i class="glyphicon glyphicon-facetime-video"></i>
            Lesson @Model.VideoComingUp.VideoNumber/
            @Model.VideoComingUp.NumberOfVideos&nbsp;&nbsp;
            <i class="glyphicon glyphicon-time"></i>
            @Model.Video.Duration minutes
        </p>
        <div class="media-object text-dark">
            <div class="media-left">
                <img src="@Model.Genre.GenreImageUrl">
            </div>
            <div class="media-body media-middle">
                <p>@Model.Genre.GenreTitle</p>
            </div>
        </div>
    </div>
    <hr class="no-margin">
    <div class="panel-body text-dark">
        @Model.Video.Description
    </div>
</div>
