@model BandDto

<div class="instructor-bio panel">
    <div class="panel-body">
        <img src="@Model.BandImage" class="avatar img-circle" alt="">
        <h4>@Model.BandName</h4>
        <h5 class="text-primary">@Model.BandSubGenre</h5>
        <p class="text-dark">@Model.BandDescription</p>
    </div>
</div>
